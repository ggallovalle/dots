# TODO

## 2026-05-04
- [x] ui: tab at the top should show tabs with filenames
- [x] keymap: `<leader>p` paste from system clipboard `C-v` equivalent
- [SKIP] insert mode: autocomplete closing symbol

### keymap `<leader>sf`
- [x] should search git tracked files including `.` files
- [x] when not in a git repository, use the `files` picker

### keyamp `<leader>sb`
- [x] show snack picker, on select if buffer has window
   open in this or any other tab, open that window
- [x] on result list window, when `dd`, delete the hovered buffer entry

